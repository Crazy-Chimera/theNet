[build-system]
requires = ["setuptools>=68"]
build-backend = "setuptools.build_meta"

[project]
name = "thenet"
version = "0.1.0"
description = "Relational network architecture for collective computation"
requires-python = ">=3.12"

[tool.setuptools]
packages = ["src", "thenet"]
